<ion-view title="Registration" hide-back-button="true">

    <ion-nav-buttons side="left">
        <button menu-toggle="left" class="button button-icon icon ion-navicon"></button>
    </ion-nav-buttons>

    <ion-nav-buttons side="right">
        <div style="text-align:right;margin-right:5px">
            <img src="img/logo.png" width="38" height="28" />
            <div class="signin">
                <ul ng-if="userModel.isLogIn==false">
                    <li>
                        <a href="#/app/login">Login</a>
                    </li>
                </ul>
                <ul ng-if="userModel.isLogIn==true">
                    <li>{{userModel.firstname}}</li>
                </ul>
            </div>
        </div>
    </ion-nav-buttons>

    <ion-content has-header="true" class="content-bg"  >

        <div class="list list-inset content-bg " style="padding:10px">
            <div ng-if="loginAlerts.length>0">
                <div class="alert alert-{{alert.type}}" ng-repeat="alert in loginAlerts">{{alert.message}}</div>
            </div>
            <form role="form" name="myForm" class="content-bg-wrapper" >
		    
                <input type="text" class="form-control" name="firstname" placeholder="Enter first name" ng-model="user.firstname" required>
                <span style="color:red" ng-show="myForm.firstname.$dirty && myForm.firstname.$invalid">
                    <span ng-show="myForm.firstname.$error.required">First name is required.</span>
                </span>
                <br />
                <input type="text" class="form-control" name="lastname" placeholder="Enter last name" ng-model="user.lastname" required>
                <span style="color:red" ng-show="myForm.lastname.$dirty && myForm.lastname.$invalid">
                    <span ng-show="myForm.lastname.$error.required">Last name is required.</span>
                </span>
                <br />
                <input type="number" class="form-control" name="mobileno" placeholder="Enter mobile number" ng-model="user.mobileno" ng-required="!user.emailid" />
                <br />
                <input type="email" class="form-control" name="emailid" placeholder="Enter email id" ng-model="user.emailid" ng-required="!user.mobileno" />
                <br />
                <input type="password" class="form-control" name="password" placeholder="Enter password" ng-model="user.password" required>
                <span style="color:red" ng-show="myForm.password.$dirty && myForm.password.$invalid">
                    <span ng-show="myForm.password.$error.required">Password is required.</span>
                </span>
                <br />
                <input type="password" class="form-control" name="repassword" placeholder="Enter re-confirm password" ng-model="user.repassword" required>
                <span style="color:red" ng-show="myForm.repassword.$dirty && myForm.repassword.$invalid">
                    <span ng-show="myForm.repassword.$error.required">Re-confrim password is required.</span>
                </span>
                <br />
		<input type="checkbox" name="tems_accepted " required> <a href="#/app/terms" style="text-decoration: none; color:#3B5998">I accept the terms and conditions as specified</a>
                <br/>
		

                 <button class="buttonDarkblue" ng-click="doRegister(user)" ng-disabled="myForm.password.$invalid || myForm.firstname.$invalid || myForm.lastname.$invalid|| myForm.emailid.$invalid|| myForm.mobileno.$invalid  || myForm.tems_checked.$invalid">Register</button>
		 <br/> 
            </form>

        </div>

    </ion-content>

</ion-view>
